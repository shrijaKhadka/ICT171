@model IEnumerable<ICT272.Models.Attendee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartyEvent.PartyEventID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartyEvent.PartyEventID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AttendeeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AttendeeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AttendeeID })
        </td>
    </tr>
}

</table>
